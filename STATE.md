# STATE — the now

> The overwritten snapshot of *now*. Read first by `/lesson`; written by `/close-day` and `/recalibrate`.
> Field names are English (scaffolding). Everything is indexed by session number `N` — no calendar dates.
> Weak spots do NOT live here — they live in `weak-spots.md`.

## Current session
N: 1

## Level per skill (CEFR estimate)
- reading: —
- listening: —
- writing: —
- speaking: —

> Set by the session-1 placement diagnostic and thereafter by `/recalibrate`. Target level (north star): B2/C1. A realistic landing in 6 months from a near-zero base is a solid A2–B1, with reading/grammar leading and listening/speaking lagging (exposure-gated). See `overview.md` and `LEARNING-PACE.md`.

## Current focus
(none yet — set by the session-1 placement diagnostic)

## Theme
Current theme: 1 (Begrüßung & sich vorstellen)
Sessions-in-theme: 0

> The current theme drives the session's vocabulary (the bulk) and the context of reading / writing / listening / speaking. It rides on top of `roadmap.md`; it never reorders grammar. Advanced adaptively by `/close-day` (stay ≥ ~3 sessions, ≤ ~10).

## Ledger pointer
(none yet)

> Points at the most-recent `vocab/ledger.csv` batch (by `session_tag`) once `/lesson` has logged the first vocabulary.
