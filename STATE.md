# STATE — the now

> The overwritten snapshot of *now*. Read first by `/lesson`; written by `/close-day` and `/recalibrate`.
> Field names are English (scaffolding). Everything is indexed by session number `N` — no calendar dates.
> Weak spots do NOT live here — they live in `weak-spots.md`.

## Current session
N: 5

## Level per skill (CEFR estimate)
- reading: A1
- listening: A1
- writing: A1
- speaking: A1

> Set by the session-1 placement diagnostic and thereafter by `/recalibrate`. Target level (north star): B2/C1. A realistic landing in 6 months from a near-zero base is a solid A2–B1, con reading/grammar por delante y listening/speaking rezagados (exposure-gated). Nota (no es cambio de nivel — eso lo hace `/recalibrate`): gramática muy fuerte (verbos irregulares dominados de arranque); reading lidera; listening estable en self 3/5 (cuello de botella — darle volumen).

## Current focus
**Roadmap: toca `Akkusativ`** (roadmap A1 #8) — el caso del objeto directo (*der → den*), justo lo que faltó para pedir *„Ich nehme **den** Fisch"* en el restaurante. Weak-spots a machacar en el 00-check: **género/artículo** (das Buch, das Essen — el género aún no es automático, es el punto #1), **terminación verbal según la persona** (fährt no fähre, bezahle no bezahlen), **plural `Studenten`** (n-Dekl.), y **mayúscula de sustantivos** en producción libre. Tema 2 (comida/restaurante) para vocab y contexto. Nuevo conector disponible: **aber**. Mantener el foco fonético (el learner perfecciona pronunciación en Anki con TTS — su mayor inversión de tiempo; IPA rico ayuda).

## Theme
Current theme: 2 (Essen & Restaurant)
Sessions-in-theme: 2

> The current theme drives the session's vocabulary (the bulk) and the context of reading / writing / listening / speaking. It rides on top of `roadmap.md`; it never reorders grammar. Advanced adaptively by `/close-day` (stay ≥ ~3 sessions, ≤ ~10). Se **mantiene** (apenas la 2.ª sesión del tema; piso ~3).

## Ledger pointer
session-004

> Points at the most-recent `vocab/ledger.csv` batch (by `session_tag`) once `/lesson` has logged the first vocabulary.
