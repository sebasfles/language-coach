# STATE — the now

> The overwritten snapshot of *now*. Read first by `/lesson`; written by `/close-day` and `/recalibrate`.
> Field names are English (scaffolding). Everything is indexed by session number `N` — no calendar dates.
> Weak spots do NOT live here — they live in `weak-spots.md`.

## Current session
N: 4

## Level per skill (CEFR estimate)
- reading: A1
- listening: A1
- writing: A1
- speaking: A1

> Set by the session-1 placement diagnostic and thereafter by `/recalibrate`. Target level (north star): B2/C1. A realistic landing in 6 months from a near-zero base is a solid A2–B1, with reading/grammar leading and listening/speaking lagging (exposure-gated). See `overview.md` and `LEARNING-PACE.md`. Nota (no es cambio de nivel, eso lo hace `/recalibrate`): reading al 100% dos veces → va por delante; listening self 3/5 → destreza rezagada, darle volumen.

## Current focus
**Consolidar el plural** — sobre todo las **formas exactas** que fallaron (*Studenten* [n-Dekl.], *Schwestern* [-er→+n], *Zahlen* [fem.→-(e)n]) vía 00-check + Anki. Atender los nuevos weak-spots: **género/artículo `der` por defecto** (*das Land*) y **mayúscula de sustantivos** (Familie/Freunde/Buch). Cerrar los watches que están a un paso: **número/concordancia**, **Stadt/Land**, **ist vs is** (una reaparición correcta más cada uno). Avanzar el roadmap A1: **próximo = verbos irregulares del presente (e→i / a→ä)** — ya asomó con *spricht*; luego Akkusativ. Nuevo tema (comida/restaurante) para el vocab y el contexto. Apoyar la fortaleza fonética (IPA, umlauts en plurales, shadowing).

## Theme
Current theme: 2 (Essen & Restaurant)
Sessions-in-theme: 1

> The current theme drives the session's vocabulary (the bulk) and the context of reading / writing / listening / speaking. It rides on top of `roadmap.md`; it never reorders grammar. Advanced adaptively by `/close-day` (stay ≥ ~3 sessions, ≤ ~10). **Avanzó 1→2 al cierre de la s3**: tema 1 (Begrüßung) completado en 3 sesiones (piso), vocab sólido, lectura 100%, sin errores mayores. Sub-temas del tema 1 pendientes (Sie/du, números 0–100, deletrear el nombre) se revisitarán.

## Ledger pointer
session-003

> Points at the most-recent `vocab/ledger.csv` batch (by `session_tag`) once `/lesson` has logged the first vocabulary.
