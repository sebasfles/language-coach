# LEARNING-PACE — throughput only

> **Throughput only.** Per-segment prescribed vs completed load, self-reported time/difficulty, and one per-segment load multiplier.
> This file carries **NO** CEFR / level / roadmap opinion — that is `/recalibrate`'s job (see `progress/level-history.md` and `STATE.md`).
> Regenerated by `/close-day` on **session 1** and **every 3rd session** thereafter. Read by `/lesson` (adaptive difficulty) and `/recalibrate`.
> Indexed by session number `N` — no calendar dates. Field names are English (scaffolding).

## Schema (regenerated, not appended)

Per segment of the spine + rotating slot:

| segment | prescribed_load | completed_load | self_reported_time | self_difficulty_1to5 | load_multiplier | notes |
|---|---|---|---|---|---|---|
| 00-check | — | — | — | — | 1.0 | graded retrieval warm-up |
| 01-vocab | — | — | — | — | 1.0 | new-words batch (shared cap ~15–30) |
| 02-grammar | — | — | — | — | 1.0 | advances roadmap every session |
| 03-reading / 03-writing | — | — | — | — | 1.0 | alternating slot — whichever ran this session |
| 04-listening | — | — | — | — | 1.0 | short staple ~10–15 min; self-reported |

> Field meanings:
> - **prescribed_load** — what `/lesson` planned for the segment (items / tasks / minutes).
> - **completed_load** — what the learner actually completed.
> - **self_reported_time** — minutes the learner reports the segment took.
> - **self_difficulty_1to5** — learner's self-rated difficulty (1 = easy … 5 = very hard).
> - **load_multiplier** — one per-segment scalar `/lesson` applies to scale next session's load (>1 = give more, <1 = ease off). Flexes down when the learner reports a large Anki review backlog.

_(no data yet — first regenerated by `/close-day` at the close of session 1.)_
